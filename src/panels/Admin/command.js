import React from 'react';

class CommandForm extends React.Component {
  constructor(props) {
    super(props);
    /*   this.state = {
        commands: sampleCommands,
        matches: sampleMatches,
        tournirs: tournirs,
        currentMatch: '',
      }; */
  }

  render() {
    return <p>Команды ещё команды</p>;
  }
}

export default CommandForm;
